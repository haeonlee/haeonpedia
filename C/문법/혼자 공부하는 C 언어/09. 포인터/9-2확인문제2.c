// 09-2. 포인터 완전 정복을 위한 포인터 이해하기
// 2. 주소 값의 크기가 4바이트일 때, sizeof 연산의 결괏값이 가장 큰 것은?

/*
    char *pc;
    double *pd;

    (1) sizeof(pc) : 주소 값 크기이므로, 4바이트
    (2) sizeof(pd) : 주소 값 크기이므로, 4바이트
    (3) sizeof(*pc) : char형 변수의 크기이므로, 1바이트
    (4) sizeof(*pd) : double형 변수의 크기이므로, 8바이트
*/

